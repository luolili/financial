dependencies {
    //引入 其他模块
    compile project(":entity")
    compile project(":util")
    compile project(":swagger")
    compile project(":api")
    compile libs.mysql //mysql:cannot load  Driver
}